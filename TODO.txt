LANGUAGE FEATURES:
add values to enums (or bypass this for now with a simpler approach by using one side of the map enum equivalence)
add callable map and respective extat implementation.
add call_with_map
add comptime llvm/c abi [as a hardcoded ct variable, no comptime variables for now? or perhaps that is needed and system detects it as static]
when using C abi, just extract values from struct in order probably
reorganize evluate_structval.cpp

missing some language features:
- sizeof
- actually making internal module extern
- return type for extern (need to read the clock!)

todo:
- turn on llvm optimizations.
- eject my kernel into this repo (under benchmarks/kernel_c) - switch to llvm/clang linker.
- add benchmarking to the "C" version - this will be two or three functions going over ports.
- make two duplicates of this code (under benchmarks/kernel_ec_noopt and bechmarks/kernel_ec_withopt).
- maybe instead of duplicates i can have some conditional files, and my compilation process will choose which ones.

today: positional varaibles just to make function call loook nicer. + start on optimizations (add alwaysinline flag at callsites `func alwaysinline {...value...}`).
Monday: start on optimization for single module instance.
Tuesday: finish optimization.
Wednesday: Bencharmking.
Thursday: Finish benchmarking.
Friday: Synthesize everything.
Satruday: Submit.

MAIN GOALS:
add module system
add optimizations
add evaluation
maybe a kernel test case of writing characters to the screen (one by one) to show inlining performance diff of direct assembly execution? (or find some kernel-related thing that requires an assembly instruction).

New plan:
- implement map field mutation - need to keep track if a mapreference is mutable (based on variable) and if a function is mutable, mod will be assumed to always be mutable!
- make mut keyword on maps switch to vtable approach
- add with-phalloc-bypass ("phantom alloc")
- syntax will be "x := &MyTarget with-phalloc-bypass mod:heap_alloc(...)"

i was gonna say i should implement closures, but it is effecitvely already implemented, just use a map!

it doesnt make sense to make individual fields or functions in a map mutable.
all maps are mutable subject to their type constraints, it is the description system that would decide.
so if a string is only allowed one value, then its type must be a singleton.
as the compiler does not have such a complicated type system for now, we assume everything is mutable if the instance of the map itself is declared mutable.
