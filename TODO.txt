LANGUAGE FEATURES:
add numeric expressions
add enum types to say possibilities
add values to enums
add built-in boolean enum
make enums encode boolean and do logical stuff
add logical expressions
add comparison expressions
and if statement
add break keyword
add type modifications (via @)
add call_with_map
add comptime llvm/c abi
add log_f, log_i, log_u, log_p for debugging purposes

MAIN GOALS:
add module system
add optimizations
add evaluation
