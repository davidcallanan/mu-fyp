LANGUAGE FEATURES:
add values to enums (or bypass this for now with a simpler approach by using one side of the map enum equivalence)
add comparison expressions
and if statement
add break keyword
add type modifications (via @)
add call_with_map
add comptime llvm/c abi

MAIN GOALS:
add module system
add optimizations
add evaluation
maybe a kernel test case of writing characters to the screen (one by one) to show inlining performance diff of direct assembly execution? (or find some kernel-related thing that requires an assembly instruction).
